package homework;

public class hw_class_sphere {
}

/**
 *
 * 建構 Sphere class 滿足下列要求
 * ------------------------------
 * instance variable
 * ------------------------------
 * 1. radius (紀錄球的半徑)
 * 2. PI ( 設定 3.14 , 狀態設為 static , final )
 *
 * ------------------------------
 * methods / functions
 * ------------------------------
 * 1. constructor : no parameter , set radius to 10
 *
 * 2. constructor : one parameter( inputRadius ) , set inputRadius to radius (instance variable)
 *
 * 3. getVolume()
 *      -> parameter : no
 *      -> return : the volume of this sphere  ( 4/3*PI*R*R*R)
 *
 * 4. getVolume( ? )
 *      -> parameter : one parameter( inputRadius )
 *      -> return : the volume calculated from the input radius  ( 4/3*PI*R*R*R)
 *      -> set to 「static」
 */

